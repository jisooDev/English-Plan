version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3300:3300"
    environment:
      - DB_HOST=128.199.83.238
      - DB_DATABASE=oknumber_english_plan
      - DB_USERNAME=root
      - DB_PASSWORD=root
      - PYTHONUNBUFFERED=1
    # healthcheck:
    #   test: ["CMD", "curl", "-f", "http://localhost:70"]
    #   interval: 30s
    #   timeout: 10s
    #   retries: 3
    restart: always

